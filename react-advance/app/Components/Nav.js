import React from 'react'

const Nav = (props) => {
  return (
    <div>
        Navigation ---- 
        <h1 className='inline-block'>Data from Header: {props.num}</h1>
    </div>
  )
}

export default Nav
